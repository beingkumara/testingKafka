spring:
    data:
        mongodb:
            host: localhost
            port: 27017
            database: f1nity

auth:
  mongodb:
    host: localhost
    port: 27017
    database: authentication
    authentication-database: admin
